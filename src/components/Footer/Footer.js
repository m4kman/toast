import React from "react";

import styles from "./Footer.module.css";

function Footer() {
	return (
		<footer className={styles.wrapper}>
			<p>
				A simple implementation of a Toast UI. A React project for practice.
			</p>
		</footer>
	);
}

export default Footer;
